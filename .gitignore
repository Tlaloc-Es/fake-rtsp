# Video files — mount these via Docker volumes, never commit them
*.mp4
*.mkv
*.avi
*.mov
*.ts
*.webm
*.flv
*.wmv
*.m4v

# Keep the videos directory placeholder and the two sample clips that make
# `docker compose up` work straight after a clone
!videos/.gitkeep
!videos/parking.mp4
!videos/entrance.mp4
